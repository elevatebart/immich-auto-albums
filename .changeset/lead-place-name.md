---
"immich-auto-albums": patch
---

Name a trip after its leading place when no region carries it and that place doubles the runner-up, so a Chicago to New Orleans road trip reads "New Orleans" instead of the country. Tunable as `clustering.lead_share`, default 0.35.
