node_modules/
dist/
server/.svelte-kit/
server/build/
# Personal: homes and household names. Only the example is tracked.
/*.toml
!/config.example.toml
/config.toml.bak
/config.toml.tmp

# The key lives here. Only the example is tracked.
/.env
/.env.*
!/.env.example

__pycache__/

# Image archives.
*.tar

# The UI's mount when it runs from docker-compose: config, logs, and the key.
/data/

run_*.log
decisions_*.csv
plan_*.json
.DS_Store
