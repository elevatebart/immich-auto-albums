node_modules/
dist/
server/.svelte-kit/
server/build/
# Personal: homes and household names. Only the example is tracked.
/*.toml
!/config.example.toml
/config.toml.bak
/config.toml.tmp

# The key lives here. Only the example is tracked.
/.env
/.env.*
!/.env.example

__pycache__/

# Image archives built for the NAS import.
*.tar

run_*.log
decisions_*.csv
plan_*.json
.DS_Store
